# Core — always required
qiskit>=1.0,<2.0
qiskit-aer>=0.14
PyYAML>=6.0
requests>=2.31

# Observability — optional (enables OTLP export to Grafana / any OTEL collector)
# Install with:  pip install "quantum-ci[observability]"
# or manually:   pip install opentelemetry-api opentelemetry-sdk opentelemetry-exporter-otlp-proto-http
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
opentelemetry-exporter-otlp-proto-http>=1.20
